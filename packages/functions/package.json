{
  "name": "@zm-blood-components/functions",
  "version": "1.0.0",
  "scripts": {
    "lint": "yarn run prettier --write . && tslint --project tsconfig.json ",
    "build": "yarn run lint && yarn run test && webpack",
    "serve": "yarn run build && firebase emulators:start --only functions",
    "shell": "yarn run build && firebase functions:shell",
    "start": "yarn run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "jest --silent"
  },
  "engines": {
    "node": ">=12"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@zm-blood-components/common": "^1.0.0",
    "firebase-admin": "^8.10.0",
    "firebase-functions": "^3.6.1"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^7.0.0",
    "firebase-functions-test": "^0.2.0",
    "generate-package-json-webpack-plugin": "^2.1.2",
    "ts-loader": "^8.0.14",
    "webpack": "^5.17.0",
    "webpack-cli": "^4.4.0",
    "webpack-node-externals": "^2.5.2"
  },
  "private": true
}
